% Auto-generated TikZ coordinates from experiment results
% Regions: ['dhaka', 'maharashtra', 'mexicocity', 'southflorida']
% Embeddings: ['alphaearth', 'galileo']
% K values: [None]

% ============================================================
% DHAKA
% ============================================================

    % --- ALPHAEARTH ---
    % ALPHAEARTH-only baseline (beige dashed)
    \addplot[beige, dashed, line width=1.5pt] coordinates {
        
    };

    % ALPHAEARTH+SH L=64 (SHred)
    \addplot[SHred, mark=square, line width=1.2pt, mark size=1.5pt] coordinates {
        
    };

    % --- GALILEO ---
    % GALILEO-only baseline (beige dashed)
    \addplot[beige, dashed, line width=1.5pt] coordinates {
        
    };

    % GALILEO+SH L=64 (SHred)
    \addplot[SHred, mark=square, line width=1.2pt, mark size=1.5pt] coordinates {
        
    };


% ============================================================
% MAHARASHTRA
% ============================================================

    % --- ALPHAEARTH ---
    % ALPHAEARTH-only baseline (beige dashed)
    \addplot[beige, dashed, line width=1.5pt] coordinates {
        
    };

    % ALPHAEARTH+SH L=64 (SHred)
    \addplot[SHred, mark=square, line width=1.2pt, mark size=1.5pt] coordinates {
        
    };

    % --- GALILEO ---
    % GALILEO-only baseline (beige dashed)
    \addplot[beige, dashed, line width=1.5pt] coordinates {
        
    };

    % GALILEO+SH L=64 (SHred)
    \addplot[SHred, mark=square, line width=1.2pt, mark size=1.5pt] coordinates {
        
    };


% ============================================================
% MEXICOCITY
% ============================================================

    % --- ALPHAEARTH ---
    % ALPHAEARTH-only baseline (beige dashed)
    \addplot[beige, dashed, line width=1.5pt] coordinates {
        
    };

    % ALPHAEARTH+SH L=64 (SHred)
    \addplot[SHred, mark=square, line width=1.2pt, mark size=1.5pt] coordinates {
        
    };

    % --- GALILEO ---
    % GALILEO-only baseline (beige dashed)
    \addplot[beige, dashed, line width=1.5pt] coordinates {
        
    };

    % GALILEO+SH L=64 (SHred)
    \addplot[SHred, mark=square, line width=1.2pt, mark size=1.5pt] coordinates {
        
    };


% ============================================================
% SOUTHFLORIDA
% ============================================================

    % --- ALPHAEARTH ---
    % ALPHAEARTH-only baseline (beige dashed)
    \addplot[beige, dashed, line width=1.5pt] coordinates {
        
    };

    % ALPHAEARTH+SH L=64 (SHred)
    \addplot[SHred, mark=square, line width=1.2pt, mark size=1.5pt] coordinates {
        
    };

    % --- GALILEO ---
    % GALILEO-only baseline (beige dashed)
    \addplot[beige, dashed, line width=1.5pt] coordinates {
        
    };

    % GALILEO+SH L=64 (SHred)
    \addplot[SHred, mark=square, line width=1.2pt, mark size=1.5pt] coordinates {
        
    };

